name: Deploy MuleSoft Application Demo
on:
  push:
    branches:
      - main
 
jobs:
  call-workflow:
    uses: IndrajithKatta/pipeline/.github/workflows/pipeline-hotfix.yml@main
    secrets: inherit

